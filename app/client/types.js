Product = Constructor(
  function (type, previewImageURL, imageURL, link) {
    self.type = type;
    self.previewImageURL = previewImageURL;
    self.imageURL = imageURL;
    self.link = link;
  });
